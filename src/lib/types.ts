// All types for the news screener live in news-screener.ts (ScreenedHeadline).
// This file is intentionally minimal — kept only because some Next.js/TS configs
// reference the path. Add new shared types here as the app grows.

export {};
